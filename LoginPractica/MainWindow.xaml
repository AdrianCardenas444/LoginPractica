<Window x:Class="LoginPractica.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#171a21">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2a475e"/>
            <Setter Property="Foreground" Value="#c7d5e0"/>
            <Setter Property="BorderBrush" Value="#171a21"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2a475e"/>
            <Setter Property="Foreground" Value="#c7d5e0"/>
            <Setter Property="BorderBrush" Value="#171a21"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="SteamButton" TargetType="Button">
            <Setter Property="Background" Value="#2a475e"/>
            <Setter Property="Foreground" Value="#66c0f4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#66c0f4"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="INICIAR SESIÓN" Foreground="#66c0f4" FontSize="26" FontWeight="Bold" Margin="0,0,0,30" FontFamily="Segoe UI"/>

            <Label Content="NOMBRE DE CUENTA" Foreground="#66c0f4" FontSize="10" FontWeight="Bold"/>
            <TextBox x:Name="txtUsuario" Margin="0,0,0,15"/>

            <Label Content="CONTRASEÑA" Foreground="#66c0f4" FontSize="10" FontWeight="Bold"/>
            <PasswordBox x:Name="txtPassword" Margin="0,0,0,10"/>

            <TextBlock x:Name="lblMensaje" Text="" Foreground="#ff4c4c" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,15" TextWrapping="Wrap" TextAlignment="Center"/>

            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnSalir" Content="Salir" Grid.Column="0" Click="BtnSalir_Click" Background="Transparent" Foreground="#c7d5e0" BorderThickness="0" Cursor="Hand"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="btnRegistro" Content="Crear Cuenta" Margin="0,0,10,0" Width="100" Click="BtnRegistro_Click" Background="#3d4450" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="2"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="btnLogin" Content="Iniciar Sesión" Width="110" Style="{StaticResource SteamButton}" Click="BtnLogin_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
